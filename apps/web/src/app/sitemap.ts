import type { MetadataRoute } from "next"; const paths=["","/app","/app/borrow","/app/lend","/app/underwrite","/app/positions","/app/markets","/docs","/risk","/terms","/privacy","/cookies","/status"]; export default function sitemap():MetadataRoute.Sitemap{return paths.map(path=>({url:`https://muselend.me${path}`,lastModified:new Date(),changeFrequency:path===""?"weekly":"monthly",priority:path===""?1:.7}))}
