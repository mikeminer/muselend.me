import {NextResponse} from "next/server";import {rateLimitResponse,requestContext} from "@/lib/api";export async function GET(request:Request){const c=requestContext(request);if(c.limited)return rateLimitResponse(c.requestId);return NextResponse.json({chainId:84532,senior:null,epochs:[],source:"contracts-not-deployed",requestId:c.requestId},{headers:{"cache-control":"public, max-age=10, stale-while-revalidate=30","x-request-id":c.requestId}})}
