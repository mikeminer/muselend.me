import { notFound } from "next/navigation"; export default async function PositionPage({params}:{params:Promise<{id:string}>}){const {id}=await params;if(!/^\d+$/.test(id))notFound();return <div><p className="text-xs uppercase tracking-[.18em] text-primary">Position</p><h1 className="mt-2 font-mono text-3xl">#{id}</h1><p className="mt-4 text-muted-foreground">No deployed contract is configured. On-chain position data is unavailable.</p></div>}
